# Copy this file to .env.development (local dev) or .env.production (prod build).
# All variables must be prefixed with VITE_ to be exposed to the browser bundle.

# Base URL of the webglow-nodejs API server (no trailing slash)
# Dev:  http://localhost:3000
# Prod: https://api.yourdomain.com
VITE_API_URL=
